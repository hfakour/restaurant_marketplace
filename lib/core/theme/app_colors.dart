// lib/core/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  static const lemonGreen = Color(0xFF9ACD32); // you can tweak to match your screenshot
  static const dark = Color(0xFF1A1A1A);
  static const inactive = Colors.white70;
  static const active = Colors.white; // or gold if you want stronger accent
}

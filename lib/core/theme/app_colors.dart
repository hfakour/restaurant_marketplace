import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFD4AF37); // Gold
  static const surface = Color(0xFFFFFFFF); // White
  static const background = Color(0xFFF6F6F6); // Light gray
  static const textPrimary = Color(0xFF222222);
  static const textSecondary = Color(0xFF716D6D);
  static const muted = Color(0xFF9E9E9E);
  static const success = Color(0xFFFFC107); // Amber
  static const error = Color(0xFFD32F2F); // Red
  static const tagBg = Color(0xFFECE4D8); // Soft tag bg
  static const grayButton = Color(0xFF7A7A7A);
  static const border = Color(0xFFE0E0E0);

  // For chip icons (spicy, allergens, etc)
  static const spicy = Color(0xFFF44336); // Bright red
}
